# Ignore all saved model files with exceptions below
*.pt

# Saved models to keep. Note: when adding models here, add a description indicating why model is kept
# Our first model!
!model.pt

test.pgn
=======
# ignore all saved datasets
/datasets

# ignore all saved models
/models
